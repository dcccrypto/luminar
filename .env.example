# =============================================================================
# LUMINAR ENVIRONMENT VARIABLES
# =============================================================================
# Copy this file to .env and fill in your actual values
# Never commit .env to version control

# =============================================================================
# FRONTEND (Next.js on Vercel)
# =============================================================================

# Privy Authentication
NEXT_PUBLIC_PRIVY_APP_ID=your-privy-app-id
NEXT_PUBLIC_PRIVY_APP_SECRET=your-privy-app-secret

# Cloudflare Turnstile (Bot Protection)
NEXT_PUBLIC_TURNSTILE_SITE_KEY=your-turnstile-site-key

# Supabase (Client-side)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# =============================================================================
# BACKEND (Express on Railway)
# =============================================================================

# Privy Authentication (Server-side)
PRIVY_APP_ID=your-privy-app-id
PRIVY_APP_SECRET=your-privy-app-secret

# Cloudflare Turnstile (Server-side verification)
TURNSTILE_SECRET_KEY=your-turnstile-secret-key

# Supabase (Server-side with service role)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# Solana Configuration
RPC_URL=https://api.mainnet-beta.solana.com
VAULT_SECRET_KEY=your-vault-keypair-base58-encoded

# Admin Configuration
ADMIN_EMAIL_ALLOWLIST=admin1@example.com,admin2@example.com

# Server Configuration
PORT=3001
FRONTEND_URL=http://localhost:3000

# =============================================================================
# DEVELOPMENT NOTES
# =============================================================================

# 1. Privy Setup:
#    - Create app at https://dashboard.privy.io
#    - Get App ID and App Secret
#    - Configure embedded wallets and email auth

# 2. Turnstile Setup:
#    - Create site at https://dash.cloudflare.com
#    - Get Site Key (public) and Secret Key (private)
#    - Add domain verification

# 3. Supabase Setup:
#    - Create project at https://supabase.com
#    - Get URL and anon key from Settings > API
#    - Get service role key from Settings > API (keep secret!)
#    - Run the database migrations from the PRD

# 4. Solana Setup:
#    - Generate vault keypair: solana-keygen new --outfile vault.json
#    - Get base58 secret: solana-keygen pubkey vault.json
#    - Fund the vault with SOL for payouts
#    - Use mainnet RPC or your preferred provider

# 5. Deployment:
#    - Frontend: Deploy to Vercel with NEXT_PUBLIC_* variables
#    - Backend: Deploy to Railway with all backend variables
#    - Database: Supabase handles hosting automatically
